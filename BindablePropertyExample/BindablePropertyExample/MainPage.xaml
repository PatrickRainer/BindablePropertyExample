<?xml version="1.0" encoding="utf-8"?>

<ContentPage x:Name="MyPage"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:bindablePropertyExample="clr-namespace:BindablePropertyExample;assembly=BindablePropertyExample"
             x:Class="BindablePropertyExample.MainPage"
             BindingContext="{x:Reference Name=MyPage}">
        
    <StackLayout BackgroundColor="LightCyan">
            <Entry Text="{Binding Meeting.Subject}"/>
            <bindablePropertyExample:MeetingControl x:Name="MeetingControl" 
                Subject="{Binding Meeting.Subject}"/>
            
            <Button x:Name="TimePickerPageButton"
                    Text="Open Time Picker Demo"
                    Clicked="TimePickerPageButton_OnClicked"/>
        </StackLayout>

</ContentPage>